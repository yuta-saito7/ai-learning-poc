FROM node:22-alpine

WORKDIR /app

# 開発用の設定
ENV NODE_ENV=development

# package.json をコピー
COPY package*.json ./

# 開発依存関係も含めてインストール
RUN npm install

# ソースコードをコピー（ボリュームマウントで上書きされる）
COPY . .

# ポート公開
EXPOSE 3001

# 開発サーバー起動（watch mode）
CMD ["npm", "run", "start:dev"]
# Azure Static Web Apps 環境変数設定ガイド

# 本番環境での環境変数設定
# Azure Portal > Static Web App > 設定 > 環境変数 で以下を設定:

NUXT_PUBLIC_API_BASE=https://your-backend-api.azurewebsites.net
NODE_ENV=production

# 開発環境用 (.env.local)
NUXT_PUBLIC_API_BASE=http://localhost:3000
NODE_ENV=development
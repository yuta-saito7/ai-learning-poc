FROM node:22-alpine

WORKDIR /app

# 開発用の設定
ENV NODE_ENV=development

# package.json をコピー
COPY package*.json ./

# 依存関係をインストール
RUN npm install

# ソースコードをコピー（ボリュームマウントで上書きされる）
COPY . .

# ポート公開
EXPOSE 3000

# 開発サーバー起動
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]